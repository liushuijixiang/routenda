# Alembic

This directory marks the migration boundary for the PostgreSQL/PostGIS implementation. The demo repository is in-memory so default tests can run without Docker, but production work should add SQLAlchemy models and generated Alembic revisions here.
