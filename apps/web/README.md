# Routenda Web

Next.js operational UI for coordinators, requesters, and approvers.

```bash
cd apps/web
pnpm install
pnpm dev
pnpm lint
pnpm test
```

Set `NEXT_PUBLIC_API_BASE_URL` for browser calls and `API_INTERNAL_BASE_URL` for server-side calls from Compose.
